{
  "name": "notes-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ui": "shx rm -rf dist && cd ../introdemo && npm ci && npm run build && shx cp -r dist ../notes-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"uibuild\" && git push",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "shx": "^0.4.0"
  }
}
